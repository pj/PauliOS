#include "syscall.h"

int main(int argc, char** argv){
	//initialize();
	
	//create initial process
	//exec("echo.coff", argc, argv);
	
	// create system idle process
	//exec("idle.coff", argc, argv);
}